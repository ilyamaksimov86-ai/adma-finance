name: Patch v13
on:
  push:
    paths:
      - '.github/scripts/patch_v13.py'
      - '.github/workflows/patch-v13.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 .github/scripts/patch_v13.py
      - run: rm .github/workflows/patch-v13.yml
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add cloud.js index.html .github/workflows/patch-v13.yml
          git commit -m "Add team access management and cloud settings" || exit 0
          git push
