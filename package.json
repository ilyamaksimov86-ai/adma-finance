{
  "name": "adma-finance",
  "private": true,
  "version": "0.22.0",
  "scripts": {
    "frontend:build": "node scripts/build-frontend.mjs",
    "frontend:check": "node scripts/build-frontend.mjs --check",
    "architecture:check": "node scripts/architecture-check.mjs",
    "contracts:check": "node scripts/contract-check.mjs",
    "check": "npm run frontend:check && node --check app.js && node --check auth.js && node --check cloud.js && node --check tests/smoke.cjs && npm run architecture:check && npm run contracts:check",
    "test": "node --test tests/*.test.mjs tests/pdf.test.cjs && node tests/smoke.cjs && AUTH_MODE=web node tests/smoke.cjs"
  },
  "devDependencies": {
    "playwright": "1.62.1",
    "pdf-lib": "1.17.1",
    "@pdf-lib/fontkit": "1.1.1"
  }
}
